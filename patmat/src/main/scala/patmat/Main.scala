package patmat



/**
  * Created by mike on 07.09.16.
  */
object Main extends App {

  /*
  println(Huffman.decodedSecret.toString())

  var text : String = "test encode decode! -())(F"
  var list : List[Char] = Huffman.string2Chars(text)

  var someText: String = "The returned list should be ordered by ascending weights (i.e. the head of the list should have the smallest weight), where the weight * of a leaf is the frequency of the character."

  var a: Huffman.CodeTable = List(('a',List(0)), ('b', List(1)))
  var b: Huffman.CodeTable = List(('a',List(0)), ('b', List(0)))

  println(Huffman.mergeCodeTables(a,b))

  println(Huffman.makeOrderedLeafList(Huffman.times(Huffman.string2Chars(someText))))
  println
  println(Huffman.convert(Huffman.createCodeTree(Huffman.string2Chars(someText))))

  //println(Huffman.encode(Huffman.frenchCode)(Huffman.string2Chars("huffmanestcool")))
  var codeTree: Huffman.CodeTree = Huffman.createCodeTree(list)

  //Huffman.encode(codeTree)(list)

  println(Huffman.decode(codeTree, Huffman.encode(codeTree)(list)))

  println(Huffman.decode(codeTree, Huffman.quickEncode(codeTree)(list)))

*/

}
